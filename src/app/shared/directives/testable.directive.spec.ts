import { TestableDirective } from './testable.directive';

describe('TestableDirective', () => {
  it('should create an instance', () => {
    // const directive = new TestableDirective();
    // expect(directive).toBeTruthy();
  });
});
